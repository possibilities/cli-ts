{
  "name": "cli-ts",
  "version": "0.0.0",
  "bin": {
    "cli-ts": "./dist/index.js"
  },
  "scripts": {
    "dev": "nodemon --exec 'ncc build index.tsx' --watch index.tsx",
    "build": "ncc build index.tsx",
    "prepublish": "rm -rf dist && ncc build index.tsx"
  },
  "dependencies": {
    "@types/node": "^10.12.12",
    "@zeit/ncc": "^0.5.1",
    "minimist": "^1.2.0"
  },
  "devDependencies": {
    "typescript": "^3.2.2"
  }
}
